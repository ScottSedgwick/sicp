(ns sicp-clojure.exercise_01_05-test
  (:require [clojure.test :refer :all]
            [sicp-clojure.exercise_01_05 :refer :all]))

(deftest ex_01_05-test
  ;(testing "This will throw a StackOverflow" (is (= (ex15-test 0 (p)) 0)))
)
